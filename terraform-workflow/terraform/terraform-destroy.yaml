parameters: 
  TF_WORKSPACE:  ''

steps:
- task: Bash@3
  displayName: "terraform destroy"
  inputs:
    targetType: 'inline'
    script: | 
      $(Agent.ToolsDirectory)/terraform/terraform -chdir="$(Build.Repository.Name)/stacks" apply "terraform.tfplandestroy"
